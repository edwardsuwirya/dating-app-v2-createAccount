import XCTest
@testable import CreateAccountModuleLibrary

final class CreateAccountModuleLibraryTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
//        XCTAssertEqual(CreateAccountModuleLibrary().text, "Hello, World!")
    }
}
